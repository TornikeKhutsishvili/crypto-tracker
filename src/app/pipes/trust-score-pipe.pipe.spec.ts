import { TrustScorePipePipe } from './trust-score-pipe.pipe';

describe('TrustScorePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TrustScorePipePipe();
    expect(pipe).toBeTruthy();
  });
});
